<% pending_audit_confirmations.each do |e| %>
  <%= link_to "I confirm that I did not perform any Overtime for the week of #{e.start_date}", confirm_audit_log_path(e.id), class: "btn btn-primary btn-block btn-lg", id: "confirm_#{e.id}", data: {confirm: "Are you sure you want to confirm that you did not perform any overtime" } %> 
<% end %>